# Phase 3 Completion Audit

Date: 2026-05-08 15:19 CDT

Objective: implement Phase 3, realistic voice synthesis and injection into Google Meet.

## Audit Summary

The repository implementation for Phase 3 is complete and locally verified through fake-provider,
null-playback, WAV-dump, backend API, extension build, and preflight checks.

The full Phase 3 success criterion is not yet satisfied because the real audible Google Meet
injection test cannot run on the current machine state:

- BlackHole is not installed or visible to PortAudio.
- ElevenLabs/Cartesia credentials and a real voice ID are not configured in the environment.

## Prompt-To-Artifact Checklist

| Requirement | Artifact / command | Evidence | Status |
| --- | --- | --- | --- |
| TTS provider abstraction | `backend/tts/providers.py` | `FakeTtsProvider`, `ElevenLabsTtsProvider`, `CartesiaTtsProvider`, `create_tts_provider` | Implemented |
| Super-realistic provider path | `backend/tts/providers.py` | ElevenLabs streaming endpoint adapter and Cartesia WebSocket adapter | Implemented, credential-dependent |
| Streaming playback | `backend/tts/orchestrator.py`, `backend/tts/playback.py` | Provider chunks are written as PCM16 to `AudioPlayer.write_pcm16` while streaming | Implemented |
| Virtual mic output routing | `backend/tts/playback.py` | `SoundDeviceAudioPlayer` resolves named/indexed output devices and opens `sounddevice.RawOutputStream` | Implemented, hardware-dependent |
| BlackHole setup guidance | `README.md`, `docs/phase-3-testing.md`, `docs/adr-007-phase-3-tts-voice-injection.md` | Commands and Google Meet mic setup documented | Implemented |
| Manual speak trigger | `POST /api/tts/speak`, extension Voice card | API queues speech; extension has Speak button | Implemented |
| Manual stop/mute control | `POST /api/tts/interrupt`, extension Voice card | API interrupts current/queued speech; extension has Stop button | Implemented |
| Barge-in interruption | `backend.main._handle_endpoint_event`, `TtsOrchestrator.interrupt_current` | `speech_start` endpoint events call TTS interrupt with `human_speech` | Implemented |
| Debug audio artifacts | `PROOF_TTS_DUMP_ENABLED`, `/api/tts` recent speech `dump_path` | Runtime smoke wrote `.data/tts-smoke/57fa32bd5916417c.wav`, 22,244 bytes | Implemented |
| Device and provider preflight | `uv run verify-phase3` | Reports macOS, output devices, BlackHole cask/device, dump dir, provider credentials | Implemented |
| Local playback smoke | `uv run test-tts-playback` | Fake/null smoke produced non-zero PCM bytes | Implemented |
| Extension UI | `extension/src/shared/ControlPanel.tsx`, `useTtsStatus.ts` | Voice panel polls `/api/tts`, calls Speak and Stop endpoints | Implemented |
| Environment config | `.env.example`, `backend/config.py` | TTS provider, voice, playback, device, dump, and provider key settings | Implemented |
| Tests | `tests/test_tts.py`, `tests/test_backend_app.py`, `tests/test_verify_phase3.py` | Fake streaming, interruption, WAV dump, disabled API, endpoint barge-in wiring, verifier matching | Implemented |
| Full Google Meet audible injection | `docs/phase-3-testing.md` | Requires BlackHole visible and a real provider credential/voice; not runnable now | Blocked |

## Verification Commands

Latest successful gates:

```bash
uv run ruff check .
uv run mypy .
uv run pytest
npm run lint
npm run typecheck
npm run build
```

Latest Python test count: 33 passed.

Latest `uv run verify-phase3` output:

```text
[ok] macOS: macOS-26.3.1-arm64-arm-64bit-Mach-O
[ok] sounddevice output devices: 0:DELL S3220DGF, 1:USB Audio Device, 4:MacBook Pro Speakers
[missing] virtual mic output device: 'BlackHole' not visible to PortAudio
[warn] BlackHole Homebrew cask: not installed; brew install --cask blackhole-2ch then reboot
[ok] TTS dump directory: .data/tts
[ok] TTS provider credentials: fake provider needs no credentials
```

## Required External Steps To Complete Phase 3

1. Install BlackHole:

   ```bash
   brew install --cask blackhole-2ch
   ```

   Noninteractive install attempt on 2026-05-08 fetched the cask but failed at the privileged
   installer step because `sudo` requires an interactive password:

   ```text
   sudo: a terminal is required to read the password
   sudo: a password is required
   ```

2. Reboot macOS so the HAL driver is visible.
3. Confirm readiness:

   ```bash
   uv run verify-phase3 --device BlackHole --strict
   uv run check-audio-devices
   ```

4. Configure one real provider:

   - ElevenLabs: `ELEVENLABS_API_KEY`, `PROOF_TTS_VOICE_ID`, optional `PROOF_TTS_MODEL`.
   - Cartesia: `CARTESIA_API_KEY`, `PROOF_TTS_VOICE_ID`, optional `PROOF_TTS_MODEL`.

5. Run the full Google Meet test in `docs/phase-3-testing.md`.

## Decision

Do not mark the active goal complete until a second Meet participant or second client confirms that
speech generated by `/api/tts/speak` is audible through the BlackHole microphone path with zero TTS
processing errors and no feedback loop.
